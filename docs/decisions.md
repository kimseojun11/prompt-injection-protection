# 결정 사항 (0단계)

되돌리기 비싼 결정만 적습니다. 바꾸려면 PR + 전원 리뷰, 그리고 이 문서와 코드(`common/config.py` 등)를 같이 고칩니다.
"그거 왜 이렇게 했더라?"가 생기면 여기부터 봅니다. 발표 자료의 설계 근거로도 씁니다.

> 상태: ✅ 확정 · 📝 회의에서 기입 필요

## 1일차 회의 8개 항목

| # | 항목 | 결정 | 근거 | 상태 |
|---|---|---|---|---|
| ① | 라벨 방식 | 이진 `benign` / `injection` + `risk_score` 0~1 | "의심" 라벨을 두면 라벨러끼리 계속 어긋난다. 애매함은 점수로 표현 | ✅ |
| ② | 지원 파일 형식 | `txt` / `md` / `html` 만. **PDF 는 미지원**(여유 되면 추가) | 1단계 작업량을 한정한다. 미지원도 명시적 결정 | ✅ |
| ③ | 점수 방향 | **높을수록 위험** (`risk_score`, `final_score` 모두) | 5명이 반대로 이해하면 통합 때 전부 뒤집힌다 | ✅ |
| ④ | 청크 크기 | **384 토큰, 오버랩 50** (`config.CHUNK_SIZE_TOKENS`) | 1단계·3단계가 같은 값을 써야 한다 | ✅ (토크나이저 기준은 아래 미결 참고) |
| ⑤ | 실험 기록 | **W&B**, 프로젝트 이름 `rag-guard`, 팀 공용 계정 1개 | 무료 플랜으로 충분, MLflow보다 세팅이 빠름 | 📝 공용 계정(entity) 이름: `______` |
| ⑥ | 개발 환경 | **Python 3.11 고정**, 패키지 관리 **uv** (`uv.lock` 커밋) | clone → `uv sync` 한 번으로 5명 환경이 같아진다 | ✅ |
| ⑦ | 역할 배정 | 아래 표 | 3·4단계 담당은 2주차 말까지 2단계 데이터 작업을 돕는다 | 📝 |
| ⑧ | 정기 회의 | 주 2회 30분: `__요일 __:__`, `__요일 __:__` | 2단계 라벨링 기준 조율이 계속 필요 | 📝 |

### ⑦ 역할

| 단계 | 폴더 | 담당 |
|---|---|---|
| 1단계 전처리 | `preprocess/` | |
| 2단계 데이터 | `data/` (Git 제외) | |
| 3단계 1차 분류기 | `stage1/` | |
| 4단계 2차 LLM | `stage2/` | |
| 5단계 통합 (팀장) | `pipeline/` | |

## 0단계 구현하면서 정한 세부 규칙 (리뷰 필요)

코드에 이미 반영돼 있습니다. 이견 있으면 1주차 안에 바꾸는 게 쌉니다.

1. **좌표 규칙** — `Span` 은 `[start, end)`, 파이썬 글자 인덱스 단위.
   `Chunk.span` 은 문서 기준, `offset_map` · `evidence_span` · `DecodedSegment.span` 은 **청크 `raw_text` 기준**.
   정리본에서 찾은 위치는 반드시 `Chunk.raw_span()` 으로 원문 위치로 바꿔서 내보낸다.
2. **`offset_map` 필수** — `len(offset_map) == len(text)` 가 아니면 `Chunk` 생성 자체가 에러. "나중에 추가"를 막기 위함.
3. **모르는 필드는 에러** — 모든 양식이 `extra="forbid"`. 오타 난 필드가 조용히 무시되지 않는다.
4. **로그는 한 줄 = 한 단계**, `action` 은 최종 판정을 낸 마지막 줄에만 기록. 에스컬레이션율은 `logdb.escalation_rate()`.
5. **방어 OFF(`defense_enabled=0`)** — 판정은 그대로 돌리고 기록하되, 조치는 항상 `allow`. RAG 데모에서 "막혔을 것"을 보여줄 수 있다.
6. **라우팅·조치 정책은 임시값** (`pipeline/policy.py`, 정책명 `default-v0`) — stage1 점수 0.2~0.8 이면 stage2 로,
   최종 점수 ≥0.9 `block` / ≥0.7 `mask` / ≥0.5 `warn` / 그 외 `allow`. 5단계에서 평가 결과로 조정하고 정책명을 올린다.
7. **ID 형식** — `chunk_id = "{doc_id}-c{번호}"`, `request_id` 는 요청마다 uuid4 hex.
8. **모델 버전** — 각 단계는 `model_ver` 를 반드시 채운다(스텁은 `stub-0`). 로그에서 버전별 개선 추이를 그리는 기준.

## 미결 (다음 회의 안건)

- [ ] ④의 "384 토큰"을 **어느 토크나이저 기준**으로 셀지 (3단계 모델 토크나이저로 통일 추천)
- [ ] W&B 공용 계정 생성 및 entity 이름 확정 → `.env` 의 `WANDB_ENTITY`
- [ ] 데이터 공유 위치 (Google Drive / Hugging Face 비공개 저장소) 확정
- [ ] `rule` 단계(규칙 기반 필터)를 둘지, 둔다면 담당자
